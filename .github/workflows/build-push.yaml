name: build-push

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.x'  # Set your Python version here

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip setuptools wheel
          pip install -r requirements.txt  # Adjust this according to your requirements

      - name: Build package
        run: python setup.py sdist bdist_wheel

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          user: __token__
          password: ${{ secrets.PYPI_API_TOKEN }}  # Store your PyPI API token as a secret

      - name: Get latest version and tag
        run: |
          latest_tag=$(git describe --tags --abbrev=0)
          new_version=$(python -c "import semver; print(semver.bump_patch('$latest_tag'))")
          git tag $new_version
          git push origin $new_version
